% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/placeLabels.R
\name{.firstHit}
\alias{.firstHit}
\title{First mask-boundary hit along a leader}
\usage{
.firstHit(ax, ay, tx, ty, px, py)
}
\arguments{
\item{ax, ay}{Numeric leader-start (anchor) coordinates (vectorised).}

\item{tx, ty}{Numeric pole coordinates (shared target).}

\item{px, py}{Numeric polygon ring coordinates (implicitly closed).}
}
\value{
A list with \code{vis} (visible length) and \code{hx}, \code{hy} (the hit point).
}
\description{
For each leader (anchor -> pole) finds the first intersection with a single polygon ring,
as a fraction \code{t} in (0, 1] of the leader; the visible leader length is \code{t} times the
anchor-to-pole distance. The pole is assumed to lie inside the ring.
}
\keyword{internal}
