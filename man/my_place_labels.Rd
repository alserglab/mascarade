% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mark_label.R
\name{my_place_labels}
\alias{my_place_labels}
\title{Draw-time label placement (boundary-seed placer)}
\usage{
my_place_labels(
  rects,
  polygons,
  polygons_pad,
  bounds,
  anchors,
  simp_ratio = 0.001,
  con_type = "ledge",
  buffer = 0
)
}
\arguments{
\item{rects}{List of measured label box sizes \code{c(w, h)} in mm (a zeroed entry = not drawn).}

\item{polygons}{List of true cluster rings (\code{list(x, y)}) in mm.}

\item{polygons_pad}{List of the same rings dilated by \code{label.buffer} (the box keep-out).}

\item{bounds}{Numeric \code{c(width, height)} of the panel in mm.}

\item{anchors}{List of fallback anchor points, used only for degenerate input.}

\item{simp_ratio}{Numeric polygon-simplification fraction (see \code{simplify_outer()}).}

\item{con_type}{Leader style: \code{"ledge"}, \code{"line"}, \code{"box"}, or \code{"none"}.}

\item{buffer}{Numeric \code{label.buffer} in mm; the overflow viewport is inset by it.}
}
\value{
A list, one entry per input label: the placed centre \code{c(x, y)} in mm (\code{NULL} if not
drawn), carrying \code{attr(., "leaders")} with \code{c(ex, ey, bx, by, corner)} per drawn label.
}
\description{
Runs at draw time in the panel's millimetre space: builds the poles and the box-fit R-tree
from the cluster rings (cheap, ~20 ms for ~40 clusters; the expensive mask is not
recomputed) and calls \code{placeLabels()}. The box-fit keep-out uses the dilated polygons while
poles, leader ends and foreign-routing use the true ones, so leaders reach the real outline.
}
\keyword{internal}
