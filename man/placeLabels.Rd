% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/placeLabels.R
\name{placeLabels}
\alias{placeLabels}
\title{Place cluster labels for one view}
\usage{
placeLabels(
  geom,
  xlim,
  ylim,
  hw,
  hh,
  char_h,
  con_type = "cl",
  MU = 55,
  iters = 120L
)
}
\arguments{
\item{geom}{Box-fit structure: a list with \code{poi} (K x 2 poles), \code{rtree} (\verb{XPtr<BoxFit>}),
\code{polysx}/\code{polysy} (per-cluster rings) and optionally \code{pad_xrange} (dilated x-extent).}

\item{xlim, ylim}{Numeric length-2 viewport bounds (already inset by \code{label.buffer}).}

\item{hw, hh}{Numeric per-label box half-sizes (mm).}

\item{char_h}{Numeric line height (mm) used to scale the internal spacing constants.}

\item{con_type}{Leader style: \code{"cl"}, \code{"cm"}, or \code{"none"}.}

\item{MU}{Numeric box-spacing weight for the force polish.}

\item{iters}{Integer force-polish iteration count.}
}
\value{
A data.table (one row per cluster) with \code{cx}, \code{cy}, the box columns, the leader
anchor \code{ex}, \code{ey}, its \code{corner} flag and the visible leader end \code{bx}, \code{by}.
}
\description{
Boundary-seed placement driver: radial candidates (+ seed fallback) -> one-move sweep ->
all-pairs two-move -> continuous force polish. Conflict-free by construction given a
feasible pool. Pure given the per-label box half-sizes and line height (the draw hook
supplies those from text metrics in the panel's mm space).
}
\keyword{internal}
