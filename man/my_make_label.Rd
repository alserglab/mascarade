% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mark_label.R
\name{my_make_label}
\alias{my_make_label}
\title{Build the label + leader grobs for a mark}
\usage{
my_make_label(
  labels,
  dims,
  polygons,
  ghosts,
  buffer,
  con_type,
  con_cap,
  con_gp,
  anchor_x,
  anchor_y,
  arrow,
  simp_ratio = 0.001
)
}
\arguments{
\item{labels}{List of label-box grobs (one per mark part).}

\item{dims}{List of measured label box sizes \code{c(w, h)} in mm.}

\item{polygons}{List of cluster rings (\code{list(x, y)}) in mm.}

\item{ghosts}{Points to avoid (currently unused by the placer).}

\item{buffer}{Grid unit: the \code{label.buffer} polygon padding / box keep-out.}

\item{con_type}{Leader style: \code{"ledge"}, \code{"direct"}, or \code{"none"}.}

\item{con_cap}{Numeric gap (mm) left between the leader end and the cluster.}

\item{con_gp}{A \code{gpar} for the connectors (per drawn label).}

\item{anchor_x, anchor_y}{Optional per-label anchor overrides.}

\item{arrow}{Optional \code{grid::arrow} for the connectors.}

\item{simp_ratio}{Numeric polygon-simplification fraction (see \code{simplify_outer()}).}
}
\value{
A \code{gList}-ready list: the positioned label grobs followed by the connector grob.
}
\description{
Draw-time worker for \code{makeContent.shape_enc()}: dilates the cluster polygons by \code{buffer}
(the box keep-out), calls \code{my_place_labels()} for the placement, positions the label box
grobs and builds the leader polylines (anchor -> visible mask-boundary end, plus the
horizontal ledge for \code{con_type == "ledge"}).
}
\keyword{internal}
