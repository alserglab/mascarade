% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{packLen}
\alias{packLen}
\title{Fixed-order 1-D packing minimising total leader length}
\usage{
packLen(dx, py, h, gap, slot, ylo, yhi)
}
\arguments{
\item{dx}{Numeric horizontal pole-to-column distances, in the fixed top-to-bottom order.}

\item{py}{Numeric pole y-coordinates, in the same order.}

\item{h}{Numeric full box heights along the stacking axis, in the same order.}

\item{gap}{Numeric extra separation added between neighbouring boxes.}

\item{slot}{Numeric grid resolution (slot height) for the candidate centre positions.}

\item{ylo, yhi}{Numeric viewport y-bounds the grid must cover.}
}
\value{
Numeric vector of placed centre y-positions, aligned with the inputs.
}
\description{
For labels in a fixed top-to-bottom order, chooses stacked centre y-positions on a fine grid
that minimise the total leader length \code{sqrt(dx^2 + (cy - py)^2)}, subject to a minimum centre
separation of \code{(h_i + h_j)/2 + gap} between neighbours. Solved exactly for the given order by
a grid dynamic program (\code{O(n * gridSize)}): sweeping labels from the bottom up, \code{g[t]} is the
least total length with the current label at some slot at or below \code{t} and every lower label
packed beneath it, via a two-option transition per slot -- either place the label in slot \code{t}
(on top of the best lower label position) or skip the slot and place it lower down. Paired
with the Hungarian order (see the seed reorder) this
reproduces the assignment optimum -- exactly in the equal-height, single-line case -- because
it minimises the true Euclidean leader length rather than a squared-vertical proxy.
}
\details{
The grid spans at least the whole viewport \verb{[ylo, yhi]} (and the pole range), so labels may
use the full vertical space; when the stacked column is taller than that space the grid is
extended symmetrically beyond it so every box still fits.
}
\keyword{internal}
