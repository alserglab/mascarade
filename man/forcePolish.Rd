% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{forcePolish}
\alias{forcePolish}
\title{Continuous force-directed label polish}
\usage{
forcePolish(
  boxfit,
  cx0,
  cy0,
  hw,
  hh,
  tx,
  ty,
  pad,
  xlo,
  xhi,
  ylo,
  yhi,
  iters,
  step,
  MU,
  pad_tgt,
  stepmin,
  con_type,
  ll_hard = TRUE,
  sq = TRUE
)
}
\arguments{
\item{boxfit}{External pointer from \code{buildBoxFit()} (the cluster keep-out).}

\item{cx0, cy0}{Numeric starting label-centre coordinates.}

\item{hw, hh}{Numeric per-label box half-sizes.}

\item{tx, ty}{Numeric per-label pole (leader target).}

\item{pad}{Numeric hard box clearance.}

\item{xlo, xhi, ylo, yhi}{Numeric viewport bounds.}

\item{iters}{Integer iteration count.}

\item{step}{Numeric initial pattern-search step.}

\item{MU}{Numeric weight of the box-box spacing penalty.}

\item{pad_tgt}{Numeric target inter-box spacing.}

\item{stepmin}{Numeric smallest step tried before abandoning a direction.}

\item{con_type}{Integer leader style: 0 = "cl" (corner), otherwise "cm"/"none".}

\item{ll_hard}{Logical; if \code{TRUE}, leader-leader crossings are a hard constraint.}

\item{sq}{Logical; if \code{TRUE}, the length term uses the squared distance.}
}
\value{
A list with numeric \code{cx}, \code{cy}: the polished label centres.
}
\description{
Pattern-search descent on an energy (squared centre-to-pole length + box-box spacing +
viewport overflow) under a hard conflict guard: starting from a conflict-free layout it
only accepts conflict-free neighbours (free-space check via the BoxFit R-tree), so
feasibility is preserved. The viewport is a SOFT overflow penalty, not a hard clip, so an
off-panel label can be walked back in-bounds and a label leaves the panel only when that
lowers total energy. The leader anchor rule mirrors R's \code{.anchorPoint()}, so the conflict
guard matches the drawn geometry.
}
\keyword{internal}
