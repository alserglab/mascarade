% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/placeLabels.R
\name{.geoCols}
\alias{.geoCols}
\title{Derive box + leader geometry columns}
\usage{
.geoCols(dt, hw, hh, poi, pad, con_type)
}
\arguments{
\item{dt}{A data.table/data.frame with columns \code{label}, \code{cx}, \code{cy}.}

\item{hw, hh}{Numeric per-label box half-sizes (indexed by \code{label}).}

\item{poi}{K x 2 matrix of cluster poles.}

\item{pad}{Numeric hard box clearance added around each box.}

\item{con_type}{Leader style passed to \code{.anchorPoint()}.}
}
\value{
A data.table with the box columns, \code{ex}, \code{ey}, \code{corner} and \code{len}.
}
\description{
From a \verb{(label, cx, cy)} table build the padded box extents, the leader anchor (via
\code{.anchorPoint()}) and the ranking length (centre-to-pole distance).
}
\keyword{internal}
