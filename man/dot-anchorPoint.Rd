% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/placeLabels.R
\name{.anchorPoint}
\alias{.anchorPoint}
\title{Leader start anchor on a label box}
\usage{
.anchorPoint(cx, cy, hw, hh, tx, ty, con_type)
}
\arguments{
\item{cx, cy}{Numeric box-centre coordinates (vectorised).}

\item{hw, hh}{Numeric box half-width / half-height.}

\item{tx, ty}{Numeric pole (leader target) coordinates.}

\item{con_type}{Leader style: \code{"cl"}, \code{"cm"}, or \code{"none"}.}
}
\value{
A list with numeric \code{ex}, \code{ey} (the anchor) and logical \code{corner} (is it a corner?).
}
\description{
The point on the box border where the leader begins, aimed at the pole (\code{tx}, \code{ty}). For
\code{con_type == "cl"} this is always the sign(dx),sign(dy) quadrant corner. Otherwise (the
ggforce \code{get_end_points} rule) it is a corner only when the pole is fully to the side in
both axes; if the pole's x lies between the vertical edges it is the top/bottom edge
centre, and if its y lies between the horizontal edges it is the left/right edge centre.
}
\keyword{internal}
